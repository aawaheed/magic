
/*
 * Returns the current operating system to caller.
 */
.arguments

// Ensuring user is root.
auth.ticket.verify:root

// Retrieving operating system.
system.os

// Returning result to caller.
yield
   operating_system:x:@system.os
